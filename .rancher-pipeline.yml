stages:
- name: build
  steps:
  - runScriptConfig:
      image: busybox
      isShell: true
      shellScript: echo hi
  - runScriptConfig:
      image: busybox
      isShell: true
      shellScript: echo sleepy && sleep 15
- name: publish
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: project/test
- name: deploy
  steps:
  - applyYamlConfig:
      path: ./deployment.yaml
