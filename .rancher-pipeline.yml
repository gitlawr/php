stages:
- name: build
  steps:
  - runScriptConfig:
      image: busybox
      shellScript: |-
        echo hi
- name: publish
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: project/test
- name: deploy
  steps:
  - applyYamlConfig:
      path: ./deployment.yaml
